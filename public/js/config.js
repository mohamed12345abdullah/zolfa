const CONFIG = {
    API_URL: 'https://zolfa.vercel.app/api',
    // يمكن تغيير الـ URL حسب بيئة التشغيل
    // API_URL: window.location.hostname === 'localhost' 
    //     ? 'http://localhost:3000/api'
    //     : 'https://api.codeminds.com/api'
}; 